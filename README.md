# github-portfolio-dashboard

A live dashboard that queries the GitHub API directly for every repo in an
account: open issue count, last commit date, and whether LICENSE, README,
and .gitignore are actually present and complete, rolled up into a health
score per repo.

Work in progress.
